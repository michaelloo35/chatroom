<!DOCTYPE html>
<html>
  <head>
    <title>Hello Friend!</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
  </head>
  <body>
    <div class="container">

      #parse( $template ) ## parse( $template ) line. #parse() is a Velocity template method that 
						  ## takes a String containing the location of another .vtl file.
						  ## It will then render that template in that place.
						  ## $template has two parts. $ acts like the hash map method yourHashMap.get() 
						  ## and template acts like the key in a hash map.
    </div>
  </body>
</html>